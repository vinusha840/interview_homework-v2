type Query {
  address(username: String!): Address
  nearEarthObjects(startDate: String!, endDate: String!): NearEarthObjectFeed
}

type Mutation {
  saveAddress(username: String!, address: AddressInput!): Address
}
